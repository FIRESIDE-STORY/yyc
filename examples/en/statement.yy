const constant = 'constant'
def name = 'libai'
let year = (1 + 1 + 1) == 1

const arr = [1, 2, 3]


const one = { name: { year: { year: 1 } } }
const two = { name: { year: { year: 1 } } }

print(one)
print(two)

print(two.name)