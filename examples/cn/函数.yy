函数 弹 => (参数) {
  打印(参数)
}

函数 下 => (参数) {
  打印(参数)
}

函数 法 => (参数) {
  打印(参数)
}

函数 画 => (参数) {
  打印(参数)
}

函数 文房四宝 => (琴, 棋, 书, 画) {
  打印(琴, 棋, 书, 画)
}

定义 艺术 = 弹("琴") + 下("棋") + 法("书") + 画("画")

文房四宝("琴", "棋", "书", "画")

函数 偏科 => (打球) {
  打印(打球)
  返回
}
